defaults: &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: trainerjim
  password: trainerjim
  host: localhost
  port: 5432

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # The server defaults to notice.
  #min_messages: warning

localdev:
  <<: *defaults
  database: trainerjim_localdev

staging:
  <<: *defaults
  database: trainerjim_staging

development:
  <<: *defaults
  database: trainerjim_development

test:
  <<: *defaults
  database: trainerjim_test

production:
  <<: *defaults
  database: trainerjim_production

rok:
  <<: *defaults
  database: trainerjim_rok

igor:
  <<: *defaults
  database: trainerjim_igor

matej:
  <<: *defaults
  database: trainerjim_matej