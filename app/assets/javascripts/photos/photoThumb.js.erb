var photoThumb = angular.module('photos.photoThumb', [
  'photos.takePhotoDialog'
]);

photoThumb.directive('photoThumb', function () {
  return {
    restrict: 'E',
    scope: {
      photo: '=',
      isDeletable: '=',
      onDeleteClicked: '&',
      onPhotoClicked: '&'
    },
    templateUrl: "<%= asset_path('photos/photo-thumb.html') %>"
  };
});