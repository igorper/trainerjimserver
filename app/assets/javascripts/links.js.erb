<%# environment.context_class.instance_eval { include ApplicationHelper } %>
<%# environment.context_class.instance_eval { include Rails.application.routes.url_helpers } %>
<% urls = Rails.application.routes.url_helpers %>



////////////////////////////////////////////////////////////////////////////////
/// AUTHENTICATION
//
var login_url = '<%= urls.new_user_session_path :only_path => true %>';



////////////////////////////////////////////////////////////////////////////////
/// TRAINING STUFF
//
var training_my_templates_url = '<%= urls.training_my_templates_url :only_path => true %>';
var training_my_template_url = '<%= urls.training_my_template_url :only_path => true %>';
var training_templates_url = '<%= urls.training_templates_url :only_path => true %>';
var training_save_workout_url = '<%= urls.training_save_workout_url :only_path => true %>';
var training_delete_workout_url = '<%= urls.training_delete_workout_url :only_path => true %>';
var exercise_types_url = '<%= urls.training_exercise_types_url :only_path => true %>';


////////////////////////////////////////////////////////////////////////////////////
///// USERS
///
var users_list = '<%= urls.users_list_trainees_url :only_path => true  %>';
var trainees_url = '<%= urls.users_trainees_url :only_path => true %>';